@package
net.spy.photo.sp

@description
Get a generated key.

@sql
select currval(?) as key

@params
seq required VARCHAR The name of the sequence from which we want the key

@results
key INTEGER The last key provided by this sequence.

